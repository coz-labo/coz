{{!
    Tempalte fur *_struct.js
}}/**
 * {{desc}}
 * @memberof module:{{module}}
 * @inner
 * @constructor {{pascalcase name}}Struct
 */

"use strict";

/** @lends {{pascalcase name}}Struct */
function {{pascalcase name}}Struct(values) {
    var s = this;
    s.set(values || {});
}

{{pascalcase name}}Struct.prototype = {

    // --- Properties ---

{{#each keys}}
    /** Value of {{camelcase this}} */
    _{{camelcase this}}: undefined,
{{/each}}

    // --- Accessors ---

    /**
     * Get values.
     * @returns {object} values.
     */
    get: function () {
        var s = this;
        return {
{{#each keys}}            {{camelcase this}}: s.{{camelcase this}}(){{#if @last}}{{else}},
{{/if}}{{/each}}
        }
    },
    /**
     * Set values.
     * @param data - Data to set.
     * @returns Self.
     */
    set: function (data) {
        var s = this;
{{#each keys}}        s.{{camelcase this}}(data.{{camelcase this}});
{{/each}}
        return s;
    },
{{#each keys}}
    /**
     * Get {{camelcase this}} value.
     * @function module:{{../module}}~{{pascalcase ../name}}Struct.prototype.{{camelcase this}}
     * @returns Value of {{camelcase this}}.
     */
    /**
     * Set {{camelcase this}} value.
     * @function module:{{../module}}~{{pascalcase ../name}}Struct.prototype.{{camelcase this}}
     * @param {{camelcase this}} - Value to set.
     * @returns Self.
     */
    {{camelcase this}}: function ({{camelcase this}}) {
        var s = this;
        if (arguments.length === 0) {
            return s._{{camelcase this}};
        }
        s._{{camelcase this}} = {{camelcase this}};
        return s;
    },
{{/each}}

    // --- Meta ---

    $keys:[{{#each keys}}'{{this}}'{{#if @last}}{{else}}, {{/if}}{{/each}}]
};

module.exports = {{pascalcase name}}Struct;