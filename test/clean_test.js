/**
 * Test case for clean.
 * Runs with mocha.
 */
'use strict'

const clean = require('../lib/commands/clean.js')
const assert = require('assert')

describe('clean', () => {
  before((done) => {
    done()
  })

  after((done) => {
    done()
  })

  it('Clean', (done) => {
    done()
  })
})

/* global describe, before, after, it */
