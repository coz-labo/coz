/**
 * Bud file for test.
 */

"use strict";

var cozTmpl = require('coz-tmpl');

module.exports = cozTmpl.nodeunitTestJsBud({
    src: [
        __dirname + '/../../lib/util/flow/*.js',
        __dirname + '/../../lib/util/requiring/*.js'
    ],
    dest: __dirname
});

if (!module.parent) {
    require('../../lib').render(__filename);
}