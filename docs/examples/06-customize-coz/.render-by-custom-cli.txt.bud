/**
 * render-by-custom-cli.txt.bud
 * This is a bud file for "examples/06-customize-coz"
 */

// Custom configuration for CLI
module.exports = {
    force: true,
    mode: '444',
    tmpl: 'myCustomTmpl01',
    data: {
        'generator': require('path').basename(__filename)
    }
};