{{!
    Template for core.js
}}/**
 * Node js core modules.
 * @module kyo/lib/core{{#each modules}}
 * @property {object} {{camelcase name}} - The node.js {@link http://nodejs.org/api/{{requireName}}.html|{{requireName}} module}. {{/each}}
 */

"use strict";

module.exports = {
{{#each modules}}    get {{camelcase name}}() { return require('{{name}}'); }{{#if @last}}{{else}},
{{/if}}{{/each}}
};