{{!
    Template for ext.js
}}/**
 * External modules, installed via npm.
 * @module kyo/lib/ext{{#each modules}}
 * @property {object} {{camelcase name}} - The npm {@link https://www.npmjs.org/package/{{requireName}}|{{requireName}} module}@{{version}}. {{/each}}
 */

"use strict";

module.exports = {
{{#each modules}}    get {{camelcase name}}() { return require('{{name}}'); }{{#if @last}}{{else}},
{{/if}}{{/each}}
};