{{!
    Template for index.js
}}/**{{#each doc}}
    * @{{@key}} {{this}}{{/each}}{{#if descProperties}}{{#each modules}}
    * @property {{camelcase name}} {object} - {{#if ../doc.module}}{@link module:{{../../../doc.module}}{{#if isModule}}/{{else}}.{{/if}}{{camelcase name}}|{{camelcase name}} module}.{{/if}}{{else}}{{snakecase name}} {{/each}}
{{else}}
{{/if}}
*/

"use strict";

module.exports = {
{{#each modules}}
    get {{camelcase name}}() { return require('./{{snakecase name}}'); }{{#if @last}}{{else}},
    {{/if}}{{/each}}
};