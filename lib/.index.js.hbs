/**
 * @overview {{pkg.description}}
 * @version {{pkg.version}}
 * @module {{pkg.name}}
 * @author {@link {{pkg.author.url}}|{{pkg.author.name}}{{{END_BRACE}}}
 * @license {@link {{pkg.license}}|{{links.my_license_url}}{{{END_BRACE}}}
 * @borrows module:coz~Coz#render as render
 * @borrows module:coz~Coz#clean as clean
 * @see {{pkg.homepage}}
{{#each pkg.dependencies}} * @requires {@link https://www.npmjs.org/package/{{@key}}|{{@key}}@{{{@this}}}{{{../END_BRACE}}}
{{/each}}
 */

"use strict";

var Coz = require('./coz');

var coz = new Coz();
coz.version = '{{pkg.version}}';
coz.Coz = Coz;

module.exports = coz;