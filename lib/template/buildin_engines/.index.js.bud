"use strict";

var coz = require('../../../lib'),
    indexJsBud = coz.factory.indexJsBud;

module.exports = indexJsBud({
    dirname: __dirname,
    desc: [
        'Buildin template engine modules.'
    ],
    parentmodule: 'coz/lib/template',
    module: 'coz/lib/template/buildin_engines',
    subclasses: ['HandlebarsEngine']
});


if(!module.parent){
    coz.render(__filename);
}
