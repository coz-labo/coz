"use strict";

var leaf = require('../../../lib'),
    indexJsBud = leaf.factory.indexJsBud;

module.exports = indexJsBud({
    dirname: __dirname,
    docProperties: false,
    desc: [
        'Predefined template modules.'
    ].join('\n * '),
    doc: {
        module: 'leaf/lib/template/tmpls'
    }
});

